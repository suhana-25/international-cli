# Vercel Environment Variables Template
# Copy these to your Vercel project settings

# UploadThing Configuration
UPLOADTHING_SECRET=your_uploadthing_secret_here
UPLOADTHING_APP_ID=your_uploadthing_app_id_here
UPLOADTHING_REGION=your_uploadthing_region_here

# NextAuth Configuration (if using)
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=https://your-domain.vercel.app

# Google OAuth (if using)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Payment Configuration
PAYPAL_API_URL=https://api-m.sandbox.paypal.com
PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_CLIENT_SECRET=your_paypal_client_secret_here

# Stripe Configuration (if using)
STRIPE_SECRET_KEY=your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here

# Email Configuration (if using)
RESEND_API_KEY=your_resend_api_key_here

# Database Configuration (if using external DB)
# DATABASE_URL=your_database_url_here

# Important Notes:
# 1. Replace all "your_*_here" values with actual credentials
# 2. For production, use production URLs (not sandbox)
# 3. Keep secrets secure and never commit them to git
# 4. Test locally with .env.local before deploying
